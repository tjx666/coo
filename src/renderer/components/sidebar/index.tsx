import * as React from 'react';
import Avatar from '../avatar';
import './style.scss';

function SideBar() {
    return (
        <aside className="sidebar">
            <Avatar />
        </aside>
    );
}

export default SideBar;
